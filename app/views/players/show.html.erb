<h1 class="text-info">Jogadores</h1>

<div class="card">
  <div class="card-block">
    <h4 class="card-title"><%= @player.name %></h4>
    <p class="card-text"><%= @player.nick_name %></p>
    <%= link_to 'Editar', edit_player_path(@player), class: 'card-link' %>
  </div>
</div>
