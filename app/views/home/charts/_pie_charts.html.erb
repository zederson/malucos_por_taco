<div id="<%= id %>"> </div>

<% content_for :javascript do %>
  $(document).ready(function() {
    data = <%= raw source.to_json  %>;
    charts.pie('<%= id %>', '<%= title %>', '<%= tooltip %>', data);
  });
<% end %>
